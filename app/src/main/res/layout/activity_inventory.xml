<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    tools:ignore="ExtraText">

    <include
        layout="@layout/app_bar_inventory"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="@dimen/nav_drawer_with"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/nav_drawer_margin_top"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:menu="@menu/activity_inventory_drawer"/>

    <!--header of navigation drawer-->
    <!--app:headerLayout="@layout/nav_header_inventory"-->/>

</android.support.v4.widget.DrawerLayout>
